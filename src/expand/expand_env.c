#include "../../minishell.h"

int     expand_var_env(t_command *command, char **env)
{
    int     i;
    char    **tmp;

    i = 0;
    tmp = NULL;
    while (env[i])
    {
        
    }
    return (0);
}
